# tiktoken arrives as a transitive dependency of dspy, via litellm, which uses it purely
# as a text tokenizer with no model weights. It is a small pure Python and Rust package,
# not one of the heavy libraries (torch, transformers, sentence-transformers) this
# serving image deliberately excludes to keep cold start fast on Cloud Run.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
asyncpg==0.30.0
pgvector==0.3.6
google-genai==1.3.0
dspy==3.2.1
python-dotenv==1.0.1
httpx==0.28.1
